# from .subscriptions import deactivate_expired_subsription
# from apscheduler.schedulers.background import BackgroundScheduler
# from fastapi import BackgroundTasks


# scheduler = BackgroundScheduler()
# jobX = scheduler.add_job(deactivate_subsription, 'interval', seconds=10)

# job = scheduler.add_job(deactivate_subsription, 'cron', hour=5, minute=30)


